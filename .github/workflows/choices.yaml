name: InputsCheck

on:
  workflow_dispatch:
    inputs:
      services:
        description: "Enter service name"
        options:
          - service1
          - service2
          - service3
        type: choice
        default: service1

jobs:
  echo-data:
    runs-on: ubuntu-latest
    steps:
      - name: Echo Hello
        run: |
          echo "Hello"
