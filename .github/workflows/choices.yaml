name: InputsCheck

on:
  workflow_dispatch:
    inputs:
      services:
        type: boolean
        default: true

jobs:
  echo-data:
    runs-on: ubuntu-latest
    steps:
      - name: Echo Hello
        run: |
          echo "Hello"
